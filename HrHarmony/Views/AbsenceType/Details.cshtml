@using HrHarmony.Models.ViewModels.AbsenceType;
@model DetailsViewModel

@{
    ViewData["Title"] = "Szczegóły rodzaju absencji";
}

<h2 class="text-center">Szczegóły rodzaju absencji</h2>

<table class="table table-bordered table-hover">
    <thead class="table-dark">
        <tr>
            <th>Nazwa typu</th>
            <th>Akcje</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>@Model.TypeName</td>
            <td>
                <a class="btn btn-info btn-sm" asp-controller="AbsenceType" asp-action="Edit" asp-route-id="@Model.Id">Edytuj</a>
                <a class="btn btn-danger btn-sm" asp-controller="AbsenceType" asp-action="Delete" asp-route-id="@Model.Id">Usuń</a>
            </td>
        </tr>
    </tbody>
</table>

@if (Model.IsMainView)
{
    <a href="javascript: history.go(-1)" class="btn btn-secondary text-left">Wróć</a>
}