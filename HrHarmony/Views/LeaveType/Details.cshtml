@using HrHarmony.Data.Models.ViewModels.LeaveType;
@model DetailsViewModel

@{
    ViewData["Title"] = "Szczegóły rodzaju urlopu";
}

@if (Layout != null)
{
    <h2 class="text-center">Szczegóły rodzaju urlopu</h2>
}

<div class="table-responsive">
    <table class="table table-bordered table-hover">

        <thead class="table-dark">
            <tr>
                <th>Nazwa rodzaju</th>
                <th>Akcje</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>@Model.TypeName</td>

                <td class="d-flex">
                    <a class="btn btn-info btn-sm ms-2" asp-controller="LeaveType" asp-action="Edit" asp-route-id="@Model.Id">Edytuj</a>
                    <a class="btn btn-danger btn-sm ms-2" asp-controller="LeaveType" asp-action="Delete" asp-route-id="@Model.Id">Usuń</a>
                </td>
            </tr>
        </tbody>
    </table>
</div>

@if (Layout != null)
{
    @await Html.PartialAsync("~/Views/Shared/Records/_ComeBackButton.cshtml")

    <div class="text-center mt-3">
        <button class="btn btn-primary m-1 w-100" type="button" data-bs-toggle="collapse" data-bs-target="#multiCollapseLeaves" aria-expanded="false" aria-controls="multiCollapseLeaves">Urlopy</button>
    </div>
    <div class="row">
        <div class="col">
            <div class="collapse" id="multiCollapseLeaves">
                <div class="card card-body">
                    <h2 class="text-center">Szczegóły pracowników</h2>
                    @foreach (var leave in Model.Leaves)
                    {
                        @await Html.PartialAsync("~/Views/Leave/Details.cshtml", leave)
                    }
                </div>
            </div>
        </div>
    </div>
}